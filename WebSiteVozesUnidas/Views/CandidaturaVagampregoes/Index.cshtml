@model IEnumerable<WebSiteVozesUnidas.Models.CandidaturaVagamprego>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Candidato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vaga)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Candidato.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vaga.IdVagaEmprego)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCandidaturaVagamprego">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCandidaturaVagamprego">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCandidaturaVagamprego">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
